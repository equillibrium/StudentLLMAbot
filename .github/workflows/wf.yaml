---
name: Build docker image
on:
  push:
    branches: [ main ]
jobs:
  build:
    name: Build bot docker image

    runs-on: self-hosted

    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Docker build
        env:
          DOCKER_BUILDKIT: 1
        run: |
          docker build -t ghcr.io/studentllmabot/bot:latest .

      - name: Push Docker image
        run: |
          docker push ghcr.io/studentllmabot/bot:latest
